/// SignInUseCase — not used directly for phone/Google flows (handled by
/// PhoneAuthService / GoogleAuthService); reserved for future email auth.
library;

// Intentionally minimal — phone auth is handled via PhoneAuthService
// and Google auth via GoogleAuthService. This file is kept as a placeholder
// for future email/password sign-in support.
